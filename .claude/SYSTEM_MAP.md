# SYSTEM MAP — Liwonde Sun Hotel 2026

Built and maintained by `codebase-scout`, one domain or one tree per invocation.
Agents **cite this file instead of re-scanning the repo**. If the area you need has no
section below, that is a signal to dispatch a scout run — not to go exploring yourself.

Format per section: feature → files · entry points · DB tables touched · shared dependencies
· gaps/smells. Tables and bullets only, no prose padding.

**Status: empty. No domain has been mapped yet.** See `.claude/COVERAGE_MATRIX.md` for the
14 domains awaiting a first pass, and `.claude/CORE_SYSTEM_BRIEF.md` for the primary-file
list each scout run should start from.

---

<!-- Scout appends one `## <n>. <Domain>` section per mapping run, in domain order. -->
